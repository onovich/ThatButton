# Phase 0 Playtest Record

Date started: 2026-06-23
Phase: Phase 0 - Baseline And Playtest Criteria

This document is the source of truth for baseline playtest evidence before changing the difficulty curve. Do not use it to redesign the full curve; use it to record what the first session actually feels like.

## Acceptance Targets

- First 5 levels are learnable: a new player can explain the rule format and recover from at least one missed read.
- First 10 levels feel winnable: a player can reasonably expect progress through level 10 after one or two retries.
- Failure invites retry: after a wrong click or timeout, the player understands why they failed and is willing to restart.
- Debug reproduction exists: confusing rounds can be reproduced with a fixed seed or equivalent recorded generation data.
- Ordinary play remains direct: without debug parameters, the game starts with normal random generation.

## Test Matrix

| Surface | Required Checks | Evidence Status | Notes |
| --- | --- | --- | --- |
| iOS Safari real device | First tap unlocks audio, touch targets are accurate, safe area fits, no page scroll during play, game over restart works. | Pending real device | Record device model, iOS version, orientation, and any audio unlock delay. |
| Android Chrome real device | First tap unlocks audio, touch targets are accurate, layout fits short screens, no accidental zoom or scroll, restart works. | Pending real device | Record device model, Android/Chrome version, orientation, and touch latency. |
| Desktop browser smoke | Start game, clear at least one safe board, trigger wrong-click failure, trigger restart. | Pending local/manual | Prefer Chrome or Edge first; add Firefox if a rendering issue appears. |
| Local launcher | `StartLocalTest.ps1 -DryRun` selects a port and prints a server command. | Pending command | Full launcher opens a browser; dry-run is enough for automated baseline. |
| GitHub Pages launcher | `OpenOnlineTest.ps1 -DryRun` prints the expected Pages URL. | Pending command | Manual online play should verify the published URL after push. |
| Static validation | `npm run validate` passes. | Pending command | Checks static markers and inline JavaScript syntax. |
| Static build | `npm run build` passes. | Pending command | Confirms deployable `dist/index.html` generation. |

## Session Record Fields

Record one row per tester session.

| Field | Required | Purpose |
| --- | --- | --- |
| tester_id | Yes | Anonymous tester label, not personal information. |
| date_time | Yes | Local time of test. |
| surface | Yes | Device/browser/launcher/Pages surface from the matrix. |
| viewport | Yes | Width x height or device orientation. |
| input_mode | Yes | Mouse, trackpad, touch, or keyboard. |
| seed | When available | Reproduce confusing or representative runs. |
| highest_level | Yes | Main Phase 1 difficulty signal. |
| first_failure_level | Yes | Main early-session friction signal. |
| retry_count | Yes | Measures whether failure invites replay. |
| quit_reason | Yes | Distinguishes difficulty, unclear clue, input issue, timer pressure, or external interruption. |
| notes | Optional | Short observation from tester or observer. |

## Round Record Fields

Record one row for each notable level, failure, ambiguity, or replay-worthy round.

| Field | Required | Purpose |
| --- | --- | --- |
| level | Yes | Difficulty stage at the time of observation. |
| seed | When available | Reproduce the generated board. |
| grid_size | Yes | Current board shape; expected baseline is `3x3`. |
| fatal_count | Yes | Number of fatal buttons generated by the rule. |
| rule_text | Yes | Exact clue shown to the player. |
| time_limit_ms | Yes | Timer pressure for this level. |
| failure_reason | If failed | `wrong_click`, `timeout`, `unclear_rule`, `input_miss`, or `layout_issue`. |
| player_action | If failed | What the player clicked or did before failing. |
| ambiguity_note | If unclear | Copy issue for Phase 2, not a Phase 0 rewrite request. |
| phase_1_signal | Optional | Difficulty curve clue, such as too many safe clicks or timer too short. |
| phase_2_signal | Optional | Copywriting clue, such as a rule that reads backwards. |

## Debug Seed Entry

Add `?seed=<label>` to the local or Pages URL before starting a run. Example:

```text
http://127.0.0.1:5173/?seed=phase0-level1
```

When `seed` is absent, the game keeps using normal browser randomness. When `seed` is present, the same seed and level can reproduce the same generated button grid, fatal count, and rule text for design review.

For automated checks or console inspection, use:

```js
window.__THAT_BUTTON_DEBUG__.previewSeededLevel('phase0-level1', 1)
```

For design/debug sessions, add `&debug=1` after the seed or `?debug=1` without a seed. Debug mode keeps ordinary UI unchanged and writes structured round/failure events to the browser console. The same events can be inspected with:

```js
window.__THAT_BUTTON_DEBUG__.getLog()
```

## First 10-Level Review Checklist

- Levels 1-5: player identifies that the clue describes fatal buttons, not safe buttons.
- Levels 1-5: at least one success is possible without memorizing the templates.
- Levels 6-10: AND/OR/NOT rules are recognized as rule changes, not as wording mistakes.
- Levels 6-10: timer pressure feels tense but not arbitrary on desktop.
- Failure screen: player can connect the failure to the current rule text.
- Restart: player retries without needing a page refresh.

## Current Evidence Log

| Date | Surface | Result | Evidence |
| --- | --- | --- | --- |
| 2026-06-23 | Repository baseline | Pending | Phase 0 record created before implementation and command validation. |

## Data For Later Phases

Use these Phase 0 records as inputs to Phase 1:

- Highest level reached before first quit.
- First failure level and failure type.
- Fatal button count per level.
- Time limit at failure or near-failure.
- Number of safe clicks required per level.
- Whether the player failed because of rule complexity or timer pressure.

Use these Phase 0 records as inputs to Phase 2:

- Exact rule text that caused hesitation or wrong inversion.
- Whether the player read the clue as "press this" instead of "avoid this".
- Repeated terms that feel inconsistent, noisy, or too long under pressure.
- Failure feedback that did or did not explain the mistake.
